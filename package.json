{
  "name": "kiwami",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Kiwami (極) — offline-first Japanese dictionary PWA",
  "scripts": {
    "build:db": "bash scripts/build-db.sh",
    "verify:db": "node scripts/verify-db.mjs"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.1",
    "fast-xml-parser": "^5.10.1",
    "kuromoji": "^0.1.2"
  },
  "allowScripts": {
    "better-sqlite3@13.0.1": true
  }
}
